---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dominik-frontend
  namespace: praxe2026
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dominik-frontend
  template:
    metadata:
      labels:
        app: dominik-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/dominik-3/praxe-demo67-frontend:latest
          ports:
            - containerPort: 4200
---
apiVersion: v1
kind: Service
metadata:
  name: dominik-frontend
  namespace: praxe2026
spec:
  selector:
    app: dominik-frontend
  ports:
    - port: 80
      targetPort: 4200
