spring:
  application:
    name: demo67
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://dominik-mysql:3306/demo67db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:demo67admin}
    password: ${SPRING_DATASOURCE_PASSWORD:Demo67Pass1!}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

server:
  port: 8080